---
phase: 05-operational-readiness
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml
  - tools/tests/rollback-dryrun.sh
  - DOCS/ROLLBACK.md
  - .planning/artifacts/rollback-dryrun.md
  - .planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json
autonomous: true
requirements:
  - PROD-05
  - PROD-06

must_haves:
  truths:
    - "`ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` exists, follows the existing Dashboard Studio v2 pattern (`<dashboard version=\"2\" theme=\"dark\">` with JSON `<definition>`), and contains panels for: scan success rate per OS, module runtime trend, distinct hosts seen over time, event volume per host per day, module-failure heatmap, top-N truncating hosts (≥6 panels per ROADMAP success criterion 5)"
    - "AppInspect on ODIN_app_for_splunk under Enterprise scope (`--mode precert --excluded-tags cloud`) returns `summary.failure + summary.error == 0` after the new dashboard lands; JSON committed at `.planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json`"
    - "`tools/tests/rollback-dryrun.sh` exists, is executable, shellcheck-clean, exits 0 against current state, and produces `.planning/artifacts/rollback-dryrun.md` with timestamps + pre-toggle event count + post-revert event count + commit hash"
    - "`DOCS/ROLLBACK.md` exists, ≥50 lines, documents the exact Deployment Server steps to disable TA-ODIN via `disabled = true` toggle in a local overlay without removing files"
    - "`tools/tests/rollback-dryrun.sh` is added to the existing CI workflow as a hard gate (mirrors check-version-sync.sh wiring) — verified by `grep -c 'rollback-dryrun' .github/workflows/ci.yml` returning at least 1"
    - "Both inputs.conf stanzas (`[script://./bin/odin.sh]` for Linux + `[script://.\\bin\\odin.path]` for Windows) are referenced in the rollback procedure — verified by `grep -c 'odin\\.sh\\|odin\\.path' DOCS/ROLLBACK.md` returning at least 2"
    - "Phase 1+2+3+4 regression suite still green after dashboard + script additions"

threat_model:
  scope: "Adding a new Dashboard Studio v2 view to ODIN_app_for_splunk + a new shell test script + new docs. No runtime/orchestrator code changes. Dashboard is read-only (consumes index data); rollback script operates on temp inputs.conf copies."
  threats:
    - id: T1
      stride: Tampering
      description: "Malformed Dashboard Studio JSON could trip AppInspect or break Splunk view loader on install"
      severity: medium
      mitigation: "Use existing odin_overview.xml as byte-level template (proven AppInspect-clean); validate JSON inline with python3 -m json.tool before commit; AppInspect verification gate is mandatory in this plan (must_haves truth #2)"
    - id: T2
      stride: Tampering
      description: "Rollback dry-run script could accidentally modify the real inputs.conf if temp-file handling is wrong"
      severity: medium
      mitigation: "Script uses `mktemp` for the working copy and explicitly does NOT write back to TA-ODIN/default/inputs.conf. Verified by acceptance criterion: `git diff -- TA-ODIN/default/inputs.conf` is empty after script runs."
    - id: T3
      stride: Information disclosure
      description: "Dashboard panels expose hostnames and event volumes — same data already exposed by existing dashboards and saved searches"
      severity: low
      mitigation: "Already in scope of existing app; metadata/default.meta exports are unchanged; no new permission boundary"
    - id: T4
      stride: Repudiation
      description: "Rollback dry-run artifact could be tampered with after generation"
      severity: low
      mitigation: "Artifact includes git commit hash at generation time; reproducible by re-running the script against the same commit"
    - id: T5
      stride: Denial of service
      description: "Heavy SPL in dashboard panels could overload search head if widely deployed"
      severity: low
      mitigation: "Panel base searches use `earliest=-7d` or `earliest=-30d` bounded windows; tstats not used (might be added in v1.1+ for performance)"

tasks: |
  <task id="1">
    <name>Create ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml — Dashboard Studio v2 ops dashboard</name>
    <type>execute</type>
    <action>
    Create `ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` following the existing Dashboard Studio v2 pattern from `odin_overview.xml`. The file structure must be:

    ```xml
    <dashboard version="2" theme="dark">
      <label>ODIN Operational Health</label>
      <description>Fleet scan success rate, module runtime, host coverage, and truncation telemetry from TA-ODIN enumeration data.</description>
      <definition><![CDATA[
    {
      "title": "ODIN Operational Health",
      "description": "...",
      "visualizations": { ... },
      "dataSources": { ... },
      "inputs": { ... },
      "layout": {
        "type": "absolute",
        "options": { "width": 1440, "height": 1200, "display": "auto" },
        "structure": [ ... ]
      }
    }
    ]]></definition>
    </dashboard>
    ```

    Required panels (≥6 per ROADMAP success criterion 5):

    1. **Scan success rate per OS (Linux + Windows)** — 2 `splunk.singlevalue` panels side-by-side
       - Base search: `index=odin_discovery sourcetype=odin:enumeration type=odin_complete earliest=-24h | eval is_success=if(modules_failed=0,1,0) | stats avg(is_success) AS success_rate by os | eval success_pct=round(success_rate*100,1)`
       - Display per OS with sparkline trend over 24h

    2. **Module runtime p95 over time** — `splunk.timechart` (line)
       - Base search: `index=odin_discovery sourcetype=odin:enumeration type=odin_complete earliest=-7d | eval module_runtime=runtime_ms | timechart span=1h p95(module_runtime) by os`

    3. **Distinct hosts seen per day** — `splunk.timechart` (column)
       - Base search: `index=odin_discovery sourcetype=odin:enumeration type=odin_complete earliest=-30d | timechart span=1d dc(hostname) AS unique_hosts by os`

    4. **Event volume per host per day** — `splunk.timechart` (column or line)
       - Base search: `index=odin_discovery sourcetype=odin:enumeration earliest=-7d | timechart span=1d count by hostname limit=20`

    5. **Module-failure heatmap** — `splunk.table` with cell-color formatting (use `splunk.heatmap` if proven AppInspect-safe; otherwise table is fallback)
       - Base search: `index=odin_discovery sourcetype=odin:enumeration type=odin_error earliest=-7d | stats count by module hostname | sort -count`

    6. **Top-10 truncating hosts** — `splunk.table`
       - Base search: `index=odin_discovery sourcetype=odin:enumeration type=truncated earliest=-7d | stats count AS truncation_events by hostname | sort -count | head 10`

    Use `splunk.singlevalue`, `splunk.timechart`, and `splunk.table` viz components (proven AppInspect-clean per RESEARCH §1). Use `splunk.heatmap` cautiously — fall back to `splunk.table` with conditional formatting if AppInspect rejects.

    Layout: `type: absolute`, total width 1440px, height ~1200px. Reference odin_overview.xml's coordinate conventions.

    Validate JSON before commit:
    ```bash
    python3 -c "
    import re, json
    with open('ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml') as f:
        content = f.read()
    match = re.search(r'<definition><!\[CDATA\[(.*)\]\]></definition>', content, re.DOTALL)
    if not match:
        print('ERROR: no JSON definition found'); exit(1)
    json.loads(match.group(1))
    print('JSON valid')
    "
    ```
    </action>
    <read_first>
    - ODIN_app_for_splunk/default/data/ui/views/odin_overview.xml (existing v2 dashboard — byte-level template; reuse layout coordinates, viz patterns, dataSource structure)
    - .planning/phases/05-operational-readiness/05-RESEARCH.md §1 (viz components proven safe), §2 (stanza wrapper convention)
    - .planning/phases/05-operational-readiness/05-CONTEXT.md D1 (Dashboard Studio v2; existing v2 already in place per RESEARCH critical finding)
    - ODIN_app_for_splunk/metadata/default.meta (already exports [views] = system; no new export needed)
    </read_first>
    <acceptance_criteria>
    - File exists: `test -f ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` exits 0
    - File starts with `<dashboard version="2"`: `head -1 ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml | grep -q 'version="2"'` exits 0
    - Contains `<![CDATA[` JSON wrapper: `grep -c 'CDATA' ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` returns at least 1
    - JSON inside CDATA is valid: python3 JSON parse succeeds
    - Has at least 6 visualization references: `grep -cE '"type": "splunk\.[a-z]+"' ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` returns at least 6
    - Dashboard label is descriptive: `grep -c '<label>ODIN Operational Health</label>' ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` returns 1
    - References each canonical metric type: file contains `success_rate`, `module_runtime` (or `p95`), `dc(hostname)`, `truncated`, `odin_error` strings
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test -f ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml && \
      head -1 ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml | grep -q 'version="2"' && \
      python3 -c "
import re, json
with open('ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml') as f:
    content = f.read()
match = re.search(r'<definition><!\[CDATA\[(.*)\]\]></definition>', content, re.DOTALL)
if not match: exit(1)
json.loads(match.group(1))
print('valid JSON')
" && \
      test $(grep -cE '"type": "splunk\.[a-z]+"' ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml) -ge 6 && \
      grep -q '<label>ODIN Operational Health</label>' ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml && \
      echo "T1 PASS"
    </automated_verify>
  </task>

  <task id="2">
    <name>Create tools/tests/rollback-dryrun.sh — shell-fixture rollback validator</name>
    <type>execute</type>
    <action>
    Create `tools/tests/rollback-dryrun.sh` (executable, shellcheck-clean) that validates the rollback procedure WITHOUT requiring a real Splunk instance. The script must:

    1. Copy current `TA-ODIN/default/inputs.conf` to a temp file
    2. Run `bash TA-ODIN/bin/odin.sh` and capture event count + timestamp (pre-toggle baseline)
    3. Apply the rollback patch to the temp file: change `disabled = false` to `disabled = true` for both `[script://...]` stanzas
    4. Run a Python-based `configparser` parser-emulator that reads the patched temp file and verifies all `[script://...]` stanzas now have `disabled = true`
    5. Assert: emulator returns 0 (all stanzas inactive)
    6. Discard the temp patch (rollback unmodified)
    7. Re-run `bash TA-ODIN/bin/odin.sh` and capture post-revert event count + timestamp
    8. Write `.planning/artifacts/rollback-dryrun.md` with the dry-run record (timestamps, event counts, commit hash, PASS/FAIL verdict)
    9. Verify `git diff -- TA-ODIN/default/inputs.conf` is empty after the script (proves no real-file modification)
    10. Exit 0 on success

    Use the script outline from RESEARCH §4. Make it shellcheck-clean. Use `set -euo pipefail` at top. Use `mktemp` for temp files. Use `python3 -c` (NOT heredoc) to avoid the indentation pitfall from Phase 4 plan-checker finding.

    Add the script to the CI workflow `.github/workflows/ci.yml` as a new step (mirror the check-version-sync wiring pattern). Insert after the existing version-sync step and before the AppInspect steps.

    Verify induced-failure detection: temporarily corrupt `TA-ODIN/default/inputs.conf` (e.g., remove the `[script://./bin/odin.sh]` stanza), run the script, assert exit 1; revert; assert exit 0.
    </action>
    <read_first>
    - TA-ODIN/default/inputs.conf (the file being patched in temp — has 2 stanzas per RESEARCH §4)
    - tools/tests/check-version-sync.sh (shellcheck-clean style template)
    - tools/tests/check-windows-classification.sh (newest example — Phase 4; same shellcheck-clean conventions; uses `python3 -c` instead of heredocs per Phase 4 plan-checker fix)
    - .github/workflows/ci.yml (existing CI workflow — find the check-version-sync step as the insertion anchor)
    - .planning/phases/05-operational-readiness/05-RESEARCH.md §4 (full script outline)
    </read_first>
    <acceptance_criteria>
    - File exists, executable: `test -x tools/tests/rollback-dryrun.sh` exits 0
    - shellcheck clean: `shellcheck tools/tests/rollback-dryrun.sh` exits 0
    - Script exits 0 against current state: `bash tools/tests/rollback-dryrun.sh` exits 0
    - Artifact created: `test -f .planning/artifacts/rollback-dryrun.md` exits 0
    - Artifact contains git commit hash: `grep -c 'Commit:' .planning/artifacts/rollback-dryrun.md` returns at least 1
    - Artifact contains pre-toggle and post-revert counts: `grep -cE 'Pre-toggle|Post-revert|Pre-toggle event count|Post-revert event count' .planning/artifacts/rollback-dryrun.md` returns at least 2
    - Real `TA-ODIN/default/inputs.conf` unmodified: `git diff -- TA-ODIN/default/inputs.conf` is empty
    - Both stanzas covered: script's parser-emulator output mentions both `odin.sh` and `odin.path` (or all `script://` stanzas)
    - CI integration: `grep -c 'rollback-dryrun' .github/workflows/ci.yml` returns at least 1
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test -x tools/tests/rollback-dryrun.sh && \
      shellcheck tools/tests/rollback-dryrun.sh && \
      bash tools/tests/rollback-dryrun.sh && \
      test -f .planning/artifacts/rollback-dryrun.md && \
      grep -q 'Commit:' .planning/artifacts/rollback-dryrun.md && \
      git diff --quiet -- TA-ODIN/default/inputs.conf && \
      grep -q 'rollback-dryrun' .github/workflows/ci.yml && \
      echo "T2 PASS"
    </automated_verify>
  </task>

  <task id="3">
    <name>Create DOCS/ROLLBACK.md — operator-facing rollback procedure</name>
    <type>execute</type>
    <action>
    Create `DOCS/ROLLBACK.md` (≥50 lines, terse operational style matching `DOCS/ARCHITECTURE.md` and `DOCS/COMMANDS.md`).

    Required content sections:

    1. **Title + 1-line purpose**
    2. **When to roll back** — symptoms that justify rollback (alert storms, scan success rate < 50%, host enumeration overload)
    3. **Two rollback strategies:**
       - **Strategy A — Disable enumeration without uninstall** (recommended; fast, reversible). Toggle `disabled = true` in a local overlay `inputs.conf` deployed via Deployment Server. Affects both Linux (`[script://./bin/odin.sh]`) and Windows (`[script://.\bin\odin.path]`) stanzas.
       - **Strategy B — Full uninstall** (last resort). Remove `TA-ODIN` from serverclass, push empty deployment, verify cleanup on UFs.
    4. **Step-by-step Strategy A** with exact commands (deployment-apps overlay structure, splunk reload deploy-server, expected propagation time, how to verify on a target UF)
    5. **Verification** — how to confirm enumeration has stopped (Splunk search: `index=odin_discovery sourcetype=odin:enumeration earliest=-1h` should return zero new events within one scan cycle after rollback applied)
    6. **Reverting the rollback** — re-enable enumeration (set `disabled = false`, repush)
    7. **Reference to dry-run artifact** — link to `.planning/artifacts/rollback-dryrun.md` and `tools/tests/rollback-dryrun.sh` so SREs understand the procedure was tested
    8. **What this procedure does NOT cover** — explicit deferred items (real-fleet validation tracked under PROD-02; Splunk Cloud Victoria deferred; UF reload-cycle timing per-environment)

    The file should be operator-focused — assume reader knows Splunk Deployment Server, `serverclass.conf`, `splunk reload deploy-server`, etc. No tutorial-style intros.

    Acceptance: ≥50 lines, contains both stanza references, contains explicit "what's NOT tested" section, links to dry-run artifact.
    </action>
    <read_first>
    - DOCS/ARCHITECTURE.md (style template — terse, operational)
    - DOCS/COMMANDS.md (style template)
    - TA-ODIN/default/inputs.conf (the 2 stanzas being toggled)
    - .planning/phases/05-operational-readiness/05-CONTEXT.md D5 (rollback strategy + what's deferred to PROD-02)
    - .planning/phases/05-operational-readiness/05-RESEARCH.md §4 (full inputs.conf context — disabled = false vs disabled = true convention)
    </read_first>
    <acceptance_criteria>
    - File exists, ≥50 lines: `test $(wc -l < DOCS/ROLLBACK.md) -ge 50` exits 0
    - Both inputs.conf stanzas referenced: `grep -c 'odin.sh\|odin.path' DOCS/ROLLBACK.md` returns at least 2
    - Strategy A documented: `grep -ic 'strategy a\|disable enumeration\|disabled = true' DOCS/ROLLBACK.md` returns at least 1
    - Strategy B documented: `grep -ic 'strategy b\|uninstall\|remove.*serverclass' DOCS/ROLLBACK.md` returns at least 1
    - "Not covered" section present: `grep -ic 'not.*test\|not.*cover\|deferred\|prod-02' DOCS/ROLLBACK.md` returns at least 1
    - Links to dry-run artifact: `grep -c 'rollback-dryrun' DOCS/ROLLBACK.md` returns at least 1
    - References Deployment Server: `grep -ic 'deployment server\|serverclass\|reload deploy' DOCS/ROLLBACK.md` returns at least 1
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(wc -l < DOCS/ROLLBACK.md) -ge 50 && \
      test $(grep -c 'odin.sh\|odin.path' DOCS/ROLLBACK.md) -ge 2 && \
      grep -iqE 'strategy a|disable enumeration|disabled = true' DOCS/ROLLBACK.md && \
      grep -iqE 'strategy b|uninstall|remove.*serverclass' DOCS/ROLLBACK.md && \
      grep -iqE 'not.*test|not.*cover|deferred|prod-02' DOCS/ROLLBACK.md && \
      grep -q 'rollback-dryrun' DOCS/ROLLBACK.md && \
      grep -iqE 'deployment server|serverclass|reload deploy' DOCS/ROLLBACK.md && \
      echo "T3 PASS"
    </automated_verify>
  </task>

  <task id="4">
    <name>AppInspect verification + Phase 1+2+3+4 regression check</name>
    <type>execute</type>
    <action>
    Run AppInspect on `ODIN_app_for_splunk` under Enterprise scope and assert clean baseline. Run all prior-phase regression scripts.

    ```bash
    ~/Library/Python/3.9/bin/splunk-appinspect inspect ODIN_app_for_splunk --mode precert --excluded-tags cloud --output-file .planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json --data-format json
    python3 -c "import json; r=json.load(open('.planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json'))['summary']; exit(0 if r['failure']+r['error']==0 else 1)"

    bash tools/tests/check-version-sync.sh
    bash tools/tests/check-two-app-split.sh
    bash tools/tests/injection-fixtures/run.sh
    bash tools/tests/windows-parity-harness.sh
    bash tools/tests/check-windows-classification.sh
    bash tools/tests/rollback-dryrun.sh
    shellcheck tools/tests/*.sh
    ```

    All must exit 0. The new dashboard must NOT regress AppInspect; the new shell script must NOT regress shellcheck.

    Commit the AppInspect JSON artifact.
    </action>
    <read_first>
    - .planning/artifacts/appinspect/odin-app-phase04-final.json (Phase 4 baseline: failure=0, error=0, warning=0)
    </read_first>
    <acceptance_criteria>
    - File exists: `test -f .planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json` exits 0
    - failure + error == 0: `python3 -c "import json; r=json.load(open('.planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json'))['summary']; exit(0 if r['failure']+r['error']==0 else 1)"` exits 0
    - All 6 regression scripts exit 0
    - shellcheck clean on all test scripts
    - Artifact committed: `git ls-files --error-unmatch .planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json` exits 0
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      ~/Library/Python/3.9/bin/splunk-appinspect inspect ODIN_app_for_splunk --mode precert --excluded-tags cloud --output-file .planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json --data-format json >/dev/null 2>&1 && \
      python3 -c "import json; r=json.load(open('.planning/artifacts/appinspect/odin-app-phase05-wave0-plan02.json'))['summary']; print('failure=%d error=%d warning=%d' % (r['failure'], r['error'], r['warning'])); exit(0 if r['failure']+r['error']==0 else 1)" && \
      bash tools/tests/check-version-sync.sh && \
      bash tools/tests/check-two-app-split.sh && \
      bash tools/tests/injection-fixtures/run.sh && \
      bash tools/tests/windows-parity-harness.sh && \
      bash tools/tests/check-windows-classification.sh && \
      bash tools/tests/rollback-dryrun.sh && \
      shellcheck tools/tests/*.sh && \
      echo "T4 PASS"
    </automated_verify>
  </task>

verification:
  command_examples:
    - "ls ODIN_app_for_splunk/default/data/ui/views/"
    - "bash tools/tests/rollback-dryrun.sh"
    - "cat .planning/artifacts/rollback-dryrun.md"
    - "~/Library/Python/3.9/bin/splunk-appinspect inspect ODIN_app_for_splunk --mode precert --excluded-tags cloud --output-file /tmp/check.json --data-format json"
---

# Plan 05-02 — Ops Dashboard + Rollback Procedure (PROD-05 + PROD-06, Wave 0)

## Goal

Deliver two PROD-* requirements together because they share the "operator-visible artifacts" theme:

- **PROD-06** — Dashboard Studio v2 view at `ODIN_app_for_splunk/default/data/ui/views/odin_ops.xml` with ≥6 panels covering scan health, runtime, coverage, and truncation
- **PROD-05** — Documented + dry-run-validated rollback procedure: `DOCS/ROLLBACK.md` + `tools/tests/rollback-dryrun.sh` + `.planning/artifacts/rollback-dryrun.md`

## Why Wave 0

Both requirements have zero dependencies on other Phase 5 work. PROD-06 only touches `ODIN_app_for_splunk/default/data/ui/views/`. PROD-05 only touches `DOCS/`, `tools/tests/`, `.planning/artifacts/`, and `.github/workflows/ci.yml` (CI integration).

## Critical research findings (from 05-RESEARCH.md)

- **`odin_overview.xml` is already Dashboard Studio v2** (CONTEXT D1 stale assumption corrected) — the new dashboard follows the SAME pattern, no mixed-framework concern, just one consistent v2 framework
- **Two `[script://...]` stanzas in inputs.conf** — Linux (`./bin/odin.sh`) + Windows (`.\bin\odin.path`); rollback procedure must toggle BOTH
- **`disabled = false` (string convention)** — patch must use `disabled = true` (not `disabled = 1`) to stay consistent with existing style
- **`metadata/default.meta` already exports `[views] = system`** — no new export needed for `odin_ops.xml`
- **`splunk.heatmap` may not be in existing-proven set** — fall back to `splunk.table` with cell-color formatting if AppInspect rejects

## Out of scope

- Activating any alerts (D2: stays disabled-only)
- Documentation files other than ROLLBACK.md (PROD-04 owns INSTALL/TROUBLESHOOTING/DATA-DICTIONARY/UPGRADE — handled in Plan 04)
- Real Splunk dry-run (D5: deferred to PROD-02 pilot)
- Migration of `odin_overview.xml` (D1: deferred to v1.1+)
- Module-side changes (Plan 01 owns PROD-07)

## Task summary

| # | Task | Output |
|---|---|---|
| 1 | Create `odin_ops.xml` Dashboard Studio v2 view (≥6 panels) | New dashboard file with valid JSON definition |
| 2 | Create `rollback-dryrun.sh` shell-fixture validator + CI integration | Executable script + dry-run artifact + CI hard gate |
| 3 | Create `DOCS/ROLLBACK.md` operator procedure | ≥50-line operational doc covering both rollback strategies |
| 4 | AppInspect verification + Phase 1+2+3+4 regression suite | Clean baseline preserved; new artifacts don't regress prior gates |

## Threat model — see frontmatter

T1 (malformed Dashboard JSON) is the most likely real-world risk. Mitigated by: byte-level template reuse from existing v2 dashboard + JSON validation before commit + AppInspect verification gate. T2 (rollback script touching real inputs.conf) mitigated by mktemp + git-clean assertion.
