---
phase: 05-operational-readiness
plan: 04
type: execute
wave: 1
depends_on: [01, 02, 03]
files_modified:
  - DOCS/INSTALL.md
  - DOCS/TROUBLESHOOTING.md
  - DOCS/DATA-DICTIONARY.md
  - DOCS/UPGRADE.md
  - TA-ODIN/README.md
  - ODIN_app_for_splunk/README.md
autonomous: true
requirements:
  - PROD-04

must_haves:
  truths:
    - "All 4 admin docs exist, ≥50 lines each: INSTALL.md, TROUBLESHOOTING.md, DATA-DICTIONARY.md, UPGRADE.md"
    - "DATA-DICTIONARY.md has one `## type=*` subsection per emitted event type (≥10 subsections covering: odin_start, odin_complete, odin_error, truncated, service, port, package, cron, scheduled_task, process, mount)"
    - "UPGRADE.md documents v1.0.0 → v1.0.1 upgrade path including how to activate the 3 alert stanzas (cross-reference Plan 03's added SLO alert) and pointer to DOCS/ROLLBACK.md (from Plan 02)"
    - "TROUBLESHOOTING.md covers per-OS common failure modes — Linux: systemctl quirks, dpkg locks, hung NFS mounts; Windows: AppLocker/WDAC, registry permissions, scheduled-task noise, UAC edge cases"
    - "INSTALL.md is a Deployment Server rollout playbook covering both apps (TA-ODIN to UFs, ODIN_app_for_splunk to indexers/SHs) with serverclass binding examples"
    - "Both app READMEs (TA-ODIN/README.md + ODIN_app_for_splunk/README.md) reference the new docs by relative path: `grep -c '\\.\\./DOCS/' TA-ODIN/README.md ODIN_app_for_splunk/README.md` returns at least 4 (each README links to ≥2 docs)"
    - "Phase 1+2+3+4 regression suite still green (docs only — no code changes)"

threat_model:
  scope: "Pure documentation additions outside both apps' install scope. No runtime/AppInspect impact (DOCS/ is not packaged). README updates inside both apps are markdown; AppInspect tolerates README content."
  threats:
    - id: T1
      stride: Tampering
      description: "Out-of-date docs mislead operators (e.g., wrong serverclass.conf syntax, wrong cmdlet name)"
      severity: medium
      mitigation: "Cross-reference RESEARCH.md and existing CLAUDE.md/ARCHITECTURE.md/COMMANDS.md as source-of-truth for technical claims; use only commands/syntax already proven in the codebase. Plan-checker can verify references resolve to real files."
    - id: T2
      stride: Information disclosure
      description: "N/A — docs target Splunk admins, no secrets"
      severity: none
      mitigation: "N/A"
    - id: T3
      stride: Repudiation
      description: "Conflict between RUNBOOK.md (Plan 03) and TROUBLESHOOTING.md (this plan) on diagnostic SPL recommendations"
      severity: low
      mitigation: "TROUBLESHOOTING.md focuses on installation/host-level failures (e.g., 'Splunk_TA_ODIN scripts produce no events'), RUNBOOK.md focuses on alert-driven response (e.g., 'fleet success rate alert fired'). Different audience entry-points, same source-of-truth code."

tasks: |
  <task id="1">
    <name>Create DOCS/INSTALL.md — Deployment Server rollout playbook</name>
    <type>execute</type>
    <action>
    Create `DOCS/INSTALL.md` (≥50 lines, terse style matching DOCS/ARCHITECTURE.md).

    Required structure:

    1. **Purpose** (1-2 sentences) — what this doc covers + audience (Splunk admin who knows Deployment Server)
    2. **Two-app split overview** — point at CLAUDE.md and ARCHITECTURE.md; remind reader that TA-ODIN goes to UFs and ODIN_app_for_splunk goes to indexers/SHs
    3. **TA-ODIN deployment (forwarders)** — copy `TA-ODIN/` to `$SPLUNK_HOME/etc/deployment-apps/`; update `etc/system/local/serverclass.conf` to bind `TA-ODIN` to relevant serverclass; `splunk reload deploy-server`; verify on a target UF with `splunk list forward-server` and `ls $SPLUNK_HOME/etc/apps/TA-ODIN/`
    4. **ODIN_app_for_splunk deployment (indexers + search heads)** — copy to `$SPLUNK_HOME/etc/apps/`; restart Splunkd; for cluster: deploy via Cluster Manager (`splunk apply cluster-bundle`) and search-head Deployer
    5. **Verification (post-install)** — search `index=odin_discovery sourcetype=odin:enumeration` should return events within one scan cycle (~minutes for fresh install); `| inputlookup odin_host_inventory.csv` should populate after first nightly run
    6. **Per-OS prerequisites:**
       - Linux: bash 4+, coreutils (`timeout`, `awk`, `grep`); read-only access enforced by Splunk UF service account; no sudo
       - Windows: PowerShell 5.1+ built-in; UF runs as LocalSystem (default); no PS Gallery needed; `odin.path` wrapper invokes `odin.ps1` per CLAUDE.md
    7. **Serverclass binding examples** — concrete `serverclass.conf` snippets for: Linux-only fleet, Windows-only fleet, mixed fleet
    </action>
    <read_first>
    - DOCS/ARCHITECTURE.md (style template + describes the two-app split)
    - CLAUDE.md (deployment architecture canonical reference)
    - TA-ODIN/default/inputs.conf (the script:// stanzas that define what runs)
    </read_first>
    <acceptance_criteria>
    - File exists, ≥50 lines: `test $(wc -l < DOCS/INSTALL.md) -ge 50` exits 0
    - Two-app split mentioned: `grep -ic 'two-app\|TA-ODIN.*forwarder\|ODIN_app_for_splunk.*indexer\|deployment-apps' DOCS/INSTALL.md` returns at least 2
    - References both Linux and Windows: `grep -ic 'linux\|windows' DOCS/INSTALL.md` returns at least 4
    - References serverclass.conf: `grep -c 'serverclass' DOCS/INSTALL.md` returns at least 1
    - Verification step present: `grep -c 'index=odin_discovery\|inputlookup' DOCS/INSTALL.md` returns at least 1
    - References reload-cycle command: `grep -ic 'reload deploy-server\|apply cluster-bundle' DOCS/INSTALL.md` returns at least 1
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(wc -l < DOCS/INSTALL.md) -ge 50 && \
      grep -iqE 'two-app|TA-ODIN.*forwarder|deployment-apps' DOCS/INSTALL.md && \
      grep -iqE 'linux' DOCS/INSTALL.md && grep -iqE 'windows' DOCS/INSTALL.md && \
      grep -q 'serverclass' DOCS/INSTALL.md && \
      grep -qE 'index=odin_discovery|inputlookup' DOCS/INSTALL.md && \
      echo "T1 PASS"
    </automated_verify>
  </task>

  <task id="2">
    <name>Create DOCS/TROUBLESHOOTING.md — common failure modes per OS</name>
    <type>execute</type>
    <action>
    Create `DOCS/TROUBLESHOOTING.md` (≥50 lines).

    Required content (each issue as a `### Issue:` heading with symptom + diagnostic + fix):

    **Linux (≥5 issues):**
    - Issue: orchestrator never runs (Splunk UF not deploying TA-ODIN; check `splunk list forward-server`)
    - Issue: `services.sh` reports zero services (broken systemctl unit; run `systemctl list-units --failed`)
    - Issue: `packages.sh` hangs (dpkg lock — `sudo lsof /var/lib/dpkg/lock`)
    - Issue: `mounts.sh` slow on hosts with hung NFS (`df` blocks; module timeout fires; check `dmesg | grep nfs`)
    - Issue: `type=truncated` events for `processes.sh` (host has 50k+ processes; raise ODIN_MAX_EVENTS for that host)

    **Windows (≥4 issues):**
    - Issue: `odin.ps1` fails to launch under AppLocker / WDAC (publisher rule needs Splunk; coordinate with EDR/security team)
    - Issue: `packages.ps1` returns zero results (registry permission denied for HKLM\SOFTWARE\Wow6432Node — runs as LocalSystem by default but verify)
    - Issue: `scheduled_tasks.ps1` excessive noise on member servers (default Microsoft tasks dominate output; classify pattern is OK; not a bug)
    - Issue: UAC edge case on workstations causing modules to fail silently (rare; check Event Viewer Application channel for WUAUCLT errors)

    **Cross-platform (≥3 issues):**
    - Issue: events appear in Splunk but `host_role` is null (check Phase 4 lookups; specifically D-04-02 legacy role values in services.csv)
    - Issue: `odin_host_inventory.csv` is stale (nightly saved searches haven't run; check `| rest /servicesNS/-/-/saved/searches`)
    - Issue: AppInspect failure during release build (re-run with `--mode precert --excluded-tags cloud`; check JSON for specific failing rule)
    </action>
    <read_first>
    - DOCS/ARCHITECTURE.md, DOCS/COMMANDS.md (style)
    - .planning/phases/04-windows-classification-data/deferred-items.md (D-04-02 legacy role names — referenced in cross-platform issue)
    - TA-ODIN/bin/modules/*.sh (which modules exist and what they do)
    - TA-ODIN/bin/modules/*.ps1 (Windows modules)
    </read_first>
    <acceptance_criteria>
    - File exists, ≥50 lines
    - At least 5 Linux issues + 4 Windows issues + 3 cross-platform issues = ≥12 total `### Issue:` entries
    - References both bash and PowerShell modules
    - References at least 2 Phase 4 D-04-02 / classification topics
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(wc -l < DOCS/TROUBLESHOOTING.md) -ge 50 && \
      test $(grep -c '^### Issue:' DOCS/TROUBLESHOOTING.md) -ge 12 && \
      grep -iqE 'systemctl|dpkg|nfs|truncated' DOCS/TROUBLESHOOTING.md && \
      grep -iqE 'applocker|wdac|registry|scheduled.task|uac' DOCS/TROUBLESHOOTING.md && \
      grep -qiE 'host_role.*null|legacy role|D-04-02|odin_host_inventory' DOCS/TROUBLESHOOTING.md && \
      echo "T2 PASS"
    </automated_verify>
  </task>

  <task id="3">
    <name>Create DOCS/DATA-DICTIONARY.md — field reference per event type</name>
    <type>execute</type>
    <action>
    Create `DOCS/DATA-DICTIONARY.md` (≥100 lines — this is the largest doc).

    Required structure: one `## type=<name>` subsection per emitted event type. Each subsection has:
    - 1-line description of when this event fires
    - Bulleted list of every field name + 1-line meaning
    - One example event (the canonical key=value form)

    Required event types (≥10 subsections):

    1. `## type=odin_start` — orchestrator startup marker; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`
    2. `## type=odin_complete` — orchestrator completion marker; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `modules_total`, `modules_success`, `modules_failed`
    3. `## type=odin_error` — module failure; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `module`, `exit_code`, optional `message`
    4. `## type=truncated` — MAX_EVENTS cap hit; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `message`
    5. `## type=service` — Linux + Windows service enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `service_name`, `service_status`, `service_enabled`, optional `service_path`
    6. `## type=port` — listening port enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `transport`, `listen_address`, `listen_port`, optional `process_name`, `process_pid`
    7. `## type=package` — installed package enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `package_name`, `package_version`, `package_arch`, `package_manager`
    8. `## type=cron` — Linux cron/systemd-timer enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `cron_source`, `cron_user`, `cron_schedule`, `cron_command`, optional `cron_file`
    9. `## type=scheduled_task` — Windows scheduled task enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `task_name`, `task_state`, `task_author`, optional `task_action`
    10. `## type=process` — running process enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `process_pid`, `process_ppid`, `process_user`, `process_state`, `process_cpu`, `process_mem`, `process_elapsed`, `process_name`, optional `process_command`
    11. `## type=mount` — filesystem mount enumeration; fields: `timestamp`, `hostname`, `os`, `run_id`, `odin_version`, `type`, `mount_device`, `mount_point`, `mount_type`, `mount_size_kb`, `mount_used_kb`, `mount_avail_kb`, `mount_use_pct`

    For each event type, include 1 example line in canonical key=value format.

    Cross-platform parity note (top section): mention that Linux and Windows emit identical field names per event type (Phase 1 WIN-08); cron vs scheduled_task is the documented divergence per CONTEXT.md.
    </action>
    <read_first>
    - TA-ODIN/bin/odin.sh (orchestrator emit field set)
    - TA-ODIN/bin/modules/services.sh, ports.sh, packages.sh, cron.sh, processes.sh, mounts.sh (Linux field names)
    - TA-ODIN/bin/modules/services.ps1, ports.ps1, packages.ps1, scheduled_tasks.ps1, processes.ps1, mounts.ps1 (Windows field names — must match Linux per WIN-08)
    - .planning/phases/04-windows-classification-data/04-RESEARCH.md (Field-name parity table for cross-platform check)
    </read_first>
    <acceptance_criteria>
    - File exists, ≥100 lines
    - At least 11 `## type=` subsections: `grep -c '^## type=' DOCS/DATA-DICTIONARY.md` returns at least 11
    - All canonical event types present: each of `odin_start`, `odin_complete`, `odin_error`, `truncated`, `service`, `port`, `package`, `cron`, `scheduled_task`, `process`, `mount` appears as a `## type=` heading
    - At least 11 example event lines (one per type)
    - References cross-platform parity (WIN-08): `grep -ic 'parity\|identical\|same field' DOCS/DATA-DICTIONARY.md` returns at least 1
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(wc -l < DOCS/DATA-DICTIONARY.md) -ge 100 && \
      test $(grep -c '^## type=' DOCS/DATA-DICTIONARY.md) -ge 11 && \
      for t in odin_start odin_complete odin_error truncated service port package cron scheduled_task process mount; do \
        grep -q "^## type=$t" DOCS/DATA-DICTIONARY.md || { echo "missing: $t"; exit 1; }; \
      done && \
      grep -iqE 'parity|identical|same field' DOCS/DATA-DICTIONARY.md && \
      echo "T3 PASS"
    </automated_verify>
  </task>

  <task id="4">
    <name>Create DOCS/UPGRADE.md — v1.0.0 → v1.0.1 upgrade path</name>
    <type>execute</type>
    <action>
    Create `DOCS/UPGRADE.md` (≥50 lines).

    Required content:

    1. **Purpose + audience** (1 sentence)
    2. **What changed in v1.0.1** — 5 bullets covering the 7 PROD requirements: Windows classification rows (PROD-01 / Phase 4); operational dashboard (PROD-06); runbook (PROD-03); rollback procedure (PROD-05); admin docs (PROD-04); Linux module standalone-fallback hygiene (PROD-07); pilot validation (PROD-02 / Phase 6 — TBD)
    3. **Pre-upgrade checks** — verify v1.0.0 baseline (`git log --oneline v1.0.0..HEAD | head`); confirm Phase 1+2+3 regression suite passes; check `splunk-appinspect` clean baseline
    4. **Upgrade steps:**
       - Pull/merge the v1.0.1 release tag
       - Re-deploy `TA-ODIN/` via Deployment Server (no schema changes; safe in-place)
       - Re-deploy `ODIN_app_for_splunk/` via cluster manager / SH deployer
       - Restart Splunkd on indexers + SHs
       - Run `splunk apply cluster-bundle` if clustered
    5. **Post-upgrade verification:**
       - Confirm host classification still works (run host inventory saved search; should now classify Windows hosts per Phase 4)
       - Verify ops dashboard renders (`https://<splunk>/app/ODIN_app_for_splunk/odin_ops`)
       - Verify alerts are visible but disabled (`splunk btool savedsearches list --app=ODIN_app_for_splunk | grep alert_odin_`)
    6. **Activating the new SLO alert** (cross-reference DOCS/RUNBOOK.md):
       - Copy `default/savedsearches.conf` stanza to `local/savedsearches.conf`
       - Set `disabled = 0` and `cron_schedule = 0 */6 * * *` (or per-org cadence)
       - Adjust `success_rate < 0.95` threshold per-org baseline (from PROD-02 pilot data)
       - Reload: `splunk reload savedsearches`
    7. **If something goes wrong:** pointer to `DOCS/ROLLBACK.md`
    </action>
    <read_first>
    - DOCS/CHANGEHISTORY.md (existing change history format)
    - .planning/REQUIREMENTS.md (PROD-01..07 list)
    - .planning/phases/05-operational-readiness/05-RESEARCH.md §3 (alert activation steps)
    </read_first>
    <acceptance_criteria>
    - File exists, ≥50 lines
    - References v1.0.0 → v1.0.1: `grep -c 'v1.0.0\|v1.0.1' DOCS/UPGRADE.md` returns at least 2
    - References all 7 PROD-* IDs OR equivalent feature names: `grep -cE 'PROD-0|Windows classification|ops dashboard|runbook|rollback|admin docs|module.*hygiene|pilot' DOCS/UPGRADE.md` returns at least 5
    - Cross-references RUNBOOK + ROLLBACK: `grep -c 'RUNBOOK\|ROLLBACK' DOCS/UPGRADE.md` returns at least 2
    - Documents alert activation: `grep -c 'cron_schedule\|disabled = 0\|reload savedsearches' DOCS/UPGRADE.md` returns at least 1
    - References Deployment Server: `grep -ic 'deployment server\|cluster-bundle\|apply cluster' DOCS/UPGRADE.md` returns at least 1
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(wc -l < DOCS/UPGRADE.md) -ge 50 && \
      test $(grep -c 'v1.0.0\|v1.0.1' DOCS/UPGRADE.md) -ge 2 && \
      test $(grep -cE 'PROD-0|Windows classification|ops dashboard|runbook|rollback|admin docs|module.*hygiene' DOCS/UPGRADE.md) -ge 5 && \
      test $(grep -c 'RUNBOOK\|ROLLBACK' DOCS/UPGRADE.md) -ge 2 && \
      grep -qE 'cron_schedule|disabled = 0|reload savedsearches' DOCS/UPGRADE.md && \
      grep -iqE 'deployment server|cluster-bundle|apply cluster' DOCS/UPGRADE.md && \
      echo "T4 PASS"
    </automated_verify>
  </task>

  <task id="5">
    <name>Update both app READMEs with Documentation section linking to DOCS/</name>
    <type>execute</type>
    <action>
    Append a `## Documentation` section to each README:

    `TA-ODIN/README.md` and `ODIN_app_for_splunk/README.md` should each gain a section like:

    ```markdown
    ## Documentation

    - [Installation](../DOCS/INSTALL.md) — Deployment Server rollout playbook
    - [Troubleshooting](../DOCS/TROUBLESHOOTING.md) — common failure modes per OS
    - [Data Dictionary](../DOCS/DATA-DICTIONARY.md) — fields per event type
    - [Upgrade](../DOCS/UPGRADE.md) — v1.0.0 → v1.0.1 upgrade path
    - [Runbook](../DOCS/RUNBOOK.md) — on-call alert response procedures
    - [Rollback](../DOCS/ROLLBACK.md) — emergency disable procedure
    - [Architecture](../DOCS/ARCHITECTURE.md) — system overview
    ```

    Both READMEs should preserve all existing content and only ADD the new section (append at end if no Documentation section exists).
    </action>
    <read_first>
    - TA-ODIN/README.md (existing content)
    - ODIN_app_for_splunk/README.md (existing content)
    </read_first>
    <acceptance_criteria>
    - TA-ODIN/README.md has Documentation section: `grep -c '## Documentation\|## documentation' TA-ODIN/README.md` returns at least 1
    - ODIN_app_for_splunk/README.md has Documentation section: same check
    - Both link to all 6 new docs (INSTALL, TROUBLESHOOTING, DATA-DICTIONARY, UPGRADE, RUNBOOK, ROLLBACK): `grep -c '\.\./DOCS/' TA-ODIN/README.md ODIN_app_for_splunk/README.md` returns at least 12 (6 links × 2 READMEs)
    - Both READMEs unchanged in their existing content (only additions): `git diff -- TA-ODIN/README.md ODIN_app_for_splunk/README.md | grep -E '^-[^-]' | wc -l` returns 0
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      grep -iqE '## documentation' TA-ODIN/README.md && \
      grep -iqE '## documentation' ODIN_app_for_splunk/README.md && \
      test $(grep -c '\.\./DOCS/' TA-ODIN/README.md) -ge 6 && \
      test $(grep -c '\.\./DOCS/' ODIN_app_for_splunk/README.md) -ge 6 && \
      test $(git diff -- TA-ODIN/README.md ODIN_app_for_splunk/README.md | grep -E '^-[^-]' | wc -l) -eq 0 && \
      echo "T5 PASS"
    </automated_verify>
  </task>

  <task id="6">
    <name>Phase 1+2+3+4 regression check + final docs sweep</name>
    <type>execute</type>
    <action>
    Run all prior-phase regression scripts to confirm docs didn't break anything (they shouldn't — docs are outside packaged-app scope).

    ```bash
    bash tools/tests/check-version-sync.sh
    bash tools/tests/check-two-app-split.sh
    bash tools/tests/injection-fixtures/run.sh
    bash tools/tests/windows-parity-harness.sh
    bash tools/tests/check-windows-classification.sh
    bash tools/tests/rollback-dryrun.sh   # from Plan 02
    shellcheck tools/tests/*.sh
    ```

    Verify all 6 new doc files are present and ≥ROADMAP-required line count:

    ```bash
    for d in INSTALL TROUBLESHOOTING DATA-DICTIONARY UPGRADE RUNBOOK ROLLBACK; do
      lines=$(wc -l < DOCS/$d.md)
      echo "DOCS/$d.md: $lines lines"
      test "$lines" -ge 50 || exit 1
    done
    ```

    No AppInspect re-run needed — docs are outside the packaged apps. AppInspect status from Plan 02 + Plan 03 already confirmed.
    </action>
    <read_first>
    - All Phase 5 plans (01, 02, 03) summaries to confirm prior-wave artifacts are in place
    </read_first>
    <acceptance_criteria>
    - All 6 regression scripts exit 0
    - shellcheck on test scripts clean
    - All 6 new doc files exist with ≥50 lines each
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      bash tools/tests/check-version-sync.sh && \
      bash tools/tests/check-two-app-split.sh && \
      bash tools/tests/injection-fixtures/run.sh && \
      bash tools/tests/windows-parity-harness.sh && \
      bash tools/tests/check-windows-classification.sh && \
      bash tools/tests/rollback-dryrun.sh && \
      shellcheck tools/tests/*.sh && \
      for d in INSTALL TROUBLESHOOTING DATA-DICTIONARY UPGRADE RUNBOOK ROLLBACK; do \
        test $(wc -l < "DOCS/$d.md") -ge 50 || { echo "DOCS/$d.md too short"; exit 1; }; \
      done && \
      echo "T6 PASS"
    </automated_verify>
  </task>

verification:
  command_examples:
    - "ls DOCS/"
    - "for d in INSTALL TROUBLESHOOTING DATA-DICTIONARY UPGRADE RUNBOOK ROLLBACK; do printf '%-25s %s\\n' \"$d.md\" \"$(wc -l < DOCS/$d.md)\"; done"
    - "grep '\\.\\./DOCS/' TA-ODIN/README.md ODIN_app_for_splunk/README.md"
---

# Plan 05-04 — Admin Docs (PROD-04, Wave 1)

## Goal

Deliver PROD-04 by creating the 4 missing admin-facing docs + linking to all phase-5 docs from both app READMEs.

## Why Wave 1

Depends on Wave 0 plans (01, 02, 03) because:
- `DOCS/UPGRADE.md` documents how to activate the SLO alert that Plan 03 adds
- `DOCS/UPGRADE.md` references the ops dashboard that Plan 02 creates
- `DOCS/UPGRADE.md` references the rollback procedure that Plan 02 documents
- `DOCS/TROUBLESHOOTING.md` cross-references Phase 4 D-04-02 legacy role values
- READMEs link to all 6 docs (RUNBOOK + ROLLBACK from earlier plans)

Writing these before Wave 0 finishes would create stale cross-references.

## Critical research findings

- All 4 docs follow terse operational style of existing `DOCS/ARCHITECTURE.md` + `DOCS/COMMANDS.md` per CONTEXT D4
- DATA-DICTIONARY needs ≥11 `## type=` subsections (one per emitted event type)
- READMEs only get APPENDED to — preserve all existing content

## Out of scope

- Tutorial-style intros for non-Splunk admins (CONTEXT D4 audience: Splunk admins who know Deployment Server)
- New external links / Splunkbase URLs (avoid; APPI-06 grep gates only allow approved hosts)
- Updating CHANGEHISTORY.md (separate concern; planner can add a v1.0.1 entry inline if convenient)
- Module code changes (Plan 01 owns)
- Dashboard / alert / rollback files (Plans 02, 03 own)

## Task summary

| # | Task | Output |
|---|---|---|
| 1 | DOCS/INSTALL.md (Deployment Server playbook) | ≥50 lines |
| 2 | DOCS/TROUBLESHOOTING.md (≥12 issues per OS) | ≥50 lines |
| 3 | DOCS/DATA-DICTIONARY.md (≥11 event-type subsections) | ≥100 lines |
| 4 | DOCS/UPGRADE.md (v1.0.0 → v1.0.1 path) | ≥50 lines |
| 5 | Both app READMEs gain Documentation section linking to all 6 new docs | 2 README updates (additions only) |
| 6 | Phase 1+2+3+4 regression check + line-count sweep | All gates green |

## Threat model — see frontmatter
