---
phase: 05-operational-readiness
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - ODIN_app_for_splunk/default/savedsearches.conf
  - DOCS/RUNBOOK.md
  - .planning/artifacts/appinspect/odin-app-phase05-wave0-plan03.json
autonomous: true
requirements:
  - PROD-03

must_haves:
  truths:
    - "`ODIN_app_for_splunk/default/savedsearches.conf` contains a new stanza `[alert_odin_fleet_scan_success_rate_below_slo]` with `disabled = 1` and empty `cron_schedule` (definition-only per D2)"
    - "The new SLO alert stanza follows the existing alert stanza pattern (action.email = 0, alert.severity = 3, alert.suppress = 0, dispatch.earliest_time = -24h@h, etc.) — verified by `grep -A 10 '\\[alert_odin_fleet_scan_success_rate_below_slo\\]' ODIN_app_for_splunk/default/savedsearches.conf | grep -c 'disabled = 1\\|cron_schedule =$\\|alert.severity'` returning at least 3"
    - "`DOCS/RUNBOOK.md` exists, ≥100 lines, contains exactly 4 alert-response entries (one per `### ` heading per ROADMAP success criterion 1)"
    - "Each runbook entry has the 4 required subsections: 'What it means', 'Diagnostic SPL', 'Remediation', 'Escalation' — verified by `grep -c 'What it means\\|Diagnostic SPL\\|Remediation\\|Escalation' DOCS/RUNBOOK.md` returning at least 16 (4 entries × 4 subsections)"
    - "Runbook covers all 3 alert stanzas + the implicit non-124 module error case: `type=truncated`, `type=odin_error exit_code=124`, `type=odin_error` (non-124), and 'fleet scan success rate below SLO'"
    - "AppInspect ODIN_app_for_splunk under Enterprise scope returns failure=0/error=0 after the new stanza lands"
    - "Phase 1+2+3+4 regression suite still green"

threat_model:
  scope: "Adding 1 new savedsearch stanza (definition-only) + 1 new docs file. No runtime/network/auth surface; alert remains disabled until operator activates."
  threats:
    - id: T1
      stride: Tampering
      description: "Bad SPL in the SLO alert search field could break savedsearches.conf parser and disable existing searches"
      severity: medium
      mitigation: "SPL is mostly stats/eval (no expensive subsearches); validate via AppInspect (which catches savedsearches.conf parse errors). Stanza copied byte-for-byte from existing alert pattern."
    - id: T2
      stride: Repudiation
      description: "Operator activates alert without baseline data and gets false-positive storm; runbook attribution unclear"
      severity: low
      mitigation: "Stanza description explicitly tells operator to set threshold per-org after PROD-02 baseline. RUNBOOK escalation field is `<TBD per organization>` so each org's CAB owns the activation decision."
    - id: T3
      stride: Information disclosure
      description: "N/A — runbook contains operational guidance, no secrets"
      severity: none
      mitigation: "N/A"
    - id: T4
      stride: Denial of service
      description: "If alert is later activated with too-aggressive cron (e.g., * * * * *), it could overload the search head"
      severity: low
      mitigation: "Stanza ships with empty cron_schedule (operator must add); description recommends `0 */6 * * *` or per-org cadence. Search uses earliest=-24h bounded window."

tasks: |
  <task id="1">
    <name>Add alert_odin_fleet_scan_success_rate_below_slo stanza to savedsearches.conf</name>
    <type>execute</type>
    <action>
    Append a new stanza to `ODIN_app_for_splunk/default/savedsearches.conf` after the last existing alert stanza. Use the exact pattern from RESEARCH §3:

    ```ini

    [alert_odin_fleet_scan_success_rate_below_slo]
    action.email = 0
    action.script = 0
    alert.digest_mode = 1
    alert.severity = 3
    alert.suppress = 0
    alert.track = 0
    counttype = number of events
    cron_schedule =
    description = Alerts when fleet-wide TA-ODIN scan-success rate (modules_failed=0) drops below 95% over the last 24 hours. Enable by setting cron_schedule (e.g., "0 */6 * * *") and disabled = 0. Operator should set the success-rate threshold per-org after PROD-02 pilot baseline (95% is a starting point, not a hard SLO). See DOCS/RUNBOOK.md for response procedure.
    disabled = 1
    dispatch.earliest_time = -24h@h
    dispatch.latest_time = now
    search = index=odin_discovery sourcetype=odin:enumeration type=odin_complete earliest=-24h \
    | stats count(eval(modules_failed=0)) AS successful, count AS total by hostname \
    | eval success_rate=successful/total \
    | where success_rate < 0.95 \
    | stats count AS failing_hosts \
    | where failing_hosts > 0
    ```

    Append after the last existing alert stanza (preserve existing stanzas byte-for-byte). Validate file integrity with `splunk btool` if available, OR by structural grep: every `[alert_*]` stanza must still parse.
    </action>
    <read_first>
    - ODIN_app_for_splunk/default/savedsearches.conf (existing alerts: alert_odin_truncated_events, alert_odin_module_timeouts; nightly searches that follow)
    - .planning/phases/05-operational-readiness/05-RESEARCH.md §3 (full SLO alert stanza template)
    - .planning/phases/05-operational-readiness/05-CONTEXT.md D2 (definition-only strategy + 3rd alert addition)
    </read_first>
    <acceptance_criteria>
    - Stanza header present: `grep -c '^\[alert_odin_fleet_scan_success_rate_below_slo\]' ODIN_app_for_splunk/default/savedsearches.conf` returns 1
    - disabled = 1: `grep -A 12 'alert_odin_fleet_scan_success_rate_below_slo' ODIN_app_for_splunk/default/savedsearches.conf | grep -c 'disabled = 1'` returns 1
    - Empty cron_schedule: `grep -A 12 'alert_odin_fleet_scan_success_rate_below_slo' ODIN_app_for_splunk/default/savedsearches.conf | grep -cE '^cron_schedule =[ ]*$'` returns 1
    - Search references modules_failed: `grep -A 20 'alert_odin_fleet_scan_success_rate_below_slo' ODIN_app_for_splunk/default/savedsearches.conf | grep -c 'modules_failed'` returns at least 1
    - Both pre-existing alerts still present (no clobber): `grep -c '^\[alert_odin_truncated_events\]\|^\[alert_odin_module_timeouts\]' ODIN_app_for_splunk/default/savedsearches.conf` returns 2
    - Total alert stanzas now = 3: `grep -c '^\[alert_' ODIN_app_for_splunk/default/savedsearches.conf` returns 3
    - File still parses cleanly (structural grep): `awk '/^\[/{count++} END{print count}' ODIN_app_for_splunk/default/savedsearches.conf` returns at least 3 (3 alerts + N nightly searches)
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(grep -c '^\[alert_odin_fleet_scan_success_rate_below_slo\]' ODIN_app_for_splunk/default/savedsearches.conf) -eq 1 && \
      grep -A 12 'alert_odin_fleet_scan_success_rate_below_slo' ODIN_app_for_splunk/default/savedsearches.conf | grep -q 'disabled = 1' && \
      grep -A 20 'alert_odin_fleet_scan_success_rate_below_slo' ODIN_app_for_splunk/default/savedsearches.conf | grep -q 'modules_failed' && \
      test $(grep -c '^\[alert_' ODIN_app_for_splunk/default/savedsearches.conf) -eq 3 && \
      echo "T1 PASS"
    </automated_verify>
  </task>

  <task id="2">
    <name>Create DOCS/RUNBOOK.md — on-call alert response procedures (4 alert types)</name>
    <type>execute</type>
    <action>
    Create `DOCS/RUNBOOK.md` (≥100 lines, terse operational style matching `DOCS/ARCHITECTURE.md` and `DOCS/COMMANDS.md`).

    Required structure: 4 alert-response entries, each as a `### ` heading. Each entry has 4 subsections (`#### What it means`, `#### Diagnostic SPL`, `#### Remediation`, `#### Escalation`).

    The 4 entries:

    1. **`### Alert: type=truncated events** — fleet-wide MAX_EVENTS truncation
       - What it means: a host has more events than `ODIN_MAX_EVENTS` (default 50000); enumeration data is being dropped silently
       - Diagnostic SPL: `index=odin_discovery sourcetype=odin:enumeration type=truncated earliest=-7d | stats count by hostname module | sort -count`
       - Remediation: identify high-cardinality hosts (10k+ packages or processes); raise `ODIN_MAX_EVENTS` for them via Deployment Server local overlay (`local/inputs.conf` or env var); OR investigate why a host has unusual cardinality (compromised? bloated?)
       - Escalation: `<TBD per organization>` — typically platform team for Splunk forwarder config changes

    2. **`### Alert: type=odin_error exit_code=124** — module timeout (90s exceeded)
       - What it means: a single discovery module hit the per-module 90s timeout cap; that scan cycle's data for that module type is incomplete on that host
       - Diagnostic SPL: `index=odin_discovery sourcetype=odin:enumeration type=odin_error exit_code=124 earliest=-7d | stats count by hostname module | sort -count`
       - Remediation: identify the slow module (often packages.sh on hosts with hung dpkg locks, or services.sh on hosts with broken systemctl units); investigate the underlying issue; consider raising `ODIN_MODULE_TIMEOUT` if legitimately slow
       - Escalation: `<TBD per organization>` — platform team

    3. **`### Alert: type=odin_error (non-124)** — module crash / non-timeout failure
       - What it means: a discovery module exited non-zero for reasons other than timeout (permission denied, missing dependency, syntax error in environment)
       - Diagnostic SPL: `index=odin_discovery sourcetype=odin:enumeration type=odin_error NOT exit_code=124 earliest=-7d | stats count by hostname module exit_code | sort -count`
       - Remediation: read the orchestrator log for the failed module; check if module's required commands exist on the host (e.g., `systemctl` on systems without systemd); review recent OS upgrades that might have broken cmdlet availability
       - Escalation: `<TBD per organization>` — platform team or OS owner

    4. **`### Alert: fleet scan success rate below SLO** — aggregate fleet-health degradation
       - What it means: more than 5% of hosts in the fleet had `modules_failed > 0` in the last 24h; trend indicates systemic problem (deploy regression, OS patch impact, infrastructure outage)
       - Diagnostic SPL: `index=odin_discovery sourcetype=odin:enumeration type=odin_complete earliest=-24h | stats avg(eval(if(modules_failed=0,1,0))) AS success_rate by os | eval success_pct=round(success_rate*100,1)`
       - Remediation: drill into `modules_failed > 0` events to find common failure pattern; check Deployment Server logs for recent serverclass changes; validate whether `windows-parity-harness.sh` and `check-version-sync.sh` still pass against current TA-ODIN; if widespread, consider rollback per `DOCS/ROLLBACK.md`
       - Escalation: `<TBD per organization>` — platform lead + on-call SRE

    Add a top section explaining how to activate alerts (cross-reference DOCS/UPGRADE.md once that exists in Plan 04). Include a one-paragraph "When to use this runbook" intro.

    Acceptance: ≥100 lines, exactly 4 `### Alert:` headings, all 4 subsections per entry, references `DOCS/ROLLBACK.md` at least once.
    </action>
    <read_first>
    - DOCS/ARCHITECTURE.md (style template — terse, operational)
    - DOCS/COMMANDS.md (style template)
    - ODIN_app_for_splunk/default/savedsearches.conf (the 3 alert stanzas being documented)
    - .planning/phases/05-operational-readiness/05-CONTEXT.md D2 (alert activation strategy — runbook is preparatory)
    - .planning/phases/05-operational-readiness/05-RESEARCH.md §3 (alert stanza pattern + SLO SPL)
    </read_first>
    <acceptance_criteria>
    - File exists, ≥100 lines: `test $(wc -l < DOCS/RUNBOOK.md) -ge 100` exits 0
    - Exactly 4 alert-response entries: `grep -c '^### Alert:' DOCS/RUNBOOK.md` returns 4
    - Each entry has all 4 subsections (16 total): `grep -cE '^#### (What it means|Diagnostic SPL|Remediation|Escalation)' DOCS/RUNBOOK.md` returns 16
    - All 4 alert types covered: `grep -c 'type=truncated' DOCS/RUNBOOK.md` ≥1, `grep -c 'exit_code=124' DOCS/RUNBOOK.md` ≥1, `grep -ic 'fleet scan success rate\|success_rate' DOCS/RUNBOOK.md` ≥1
    - Cross-references rollback procedure: `grep -c 'ROLLBACK' DOCS/RUNBOOK.md` ≥1
    - References Deployment Server: `grep -ic 'deployment server\|serverclass' DOCS/RUNBOOK.md` ≥1
    - Escalation placeholders present: `grep -c '<TBD per organization>' DOCS/RUNBOOK.md` returns at least 4
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      test $(wc -l < DOCS/RUNBOOK.md) -ge 100 && \
      test $(grep -c '^### Alert:' DOCS/RUNBOOK.md) -eq 4 && \
      test $(grep -cE '^#### (What it means|Diagnostic SPL|Remediation|Escalation)' DOCS/RUNBOOK.md) -eq 16 && \
      grep -q 'type=truncated' DOCS/RUNBOOK.md && \
      grep -q 'exit_code=124' DOCS/RUNBOOK.md && \
      grep -iqE 'fleet scan success rate|success_rate' DOCS/RUNBOOK.md && \
      grep -q 'ROLLBACK' DOCS/RUNBOOK.md && \
      test $(grep -c '<TBD per organization>' DOCS/RUNBOOK.md) -ge 4 && \
      echo "T2 PASS"
    </automated_verify>
  </task>

  <task id="3">
    <name>AppInspect verification + Phase 1+2+3+4 regression check</name>
    <type>execute</type>
    <action>
    Run AppInspect on `ODIN_app_for_splunk` after the new stanza lands and assert clean. Run all prior-phase regression scripts.

    ```bash
    ~/Library/Python/3.9/bin/splunk-appinspect inspect ODIN_app_for_splunk --mode precert --excluded-tags cloud --output-file .planning/artifacts/appinspect/odin-app-phase05-wave0-plan03.json --data-format json
    python3 -c "import json; r=json.load(open('.planning/artifacts/appinspect/odin-app-phase05-wave0-plan03.json'))['summary']; exit(0 if r['failure']+r['error']==0 else 1)"

    bash tools/tests/check-version-sync.sh
    bash tools/tests/check-two-app-split.sh
    bash tools/tests/injection-fixtures/run.sh
    bash tools/tests/windows-parity-harness.sh
    bash tools/tests/check-windows-classification.sh
    ```

    All must exit 0. Commit the AppInspect JSON artifact.
    </action>
    <read_first>
    - .planning/artifacts/appinspect/odin-app-phase04-final.json (Phase 4 baseline)
    </read_first>
    <acceptance_criteria>
    - File exists: `test -f .planning/artifacts/appinspect/odin-app-phase05-wave0-plan03.json` exits 0
    - failure + error == 0
    - All 5 regression scripts exit 0
    </acceptance_criteria>
    <automated_verify>
    cd /Users/joehanse/Library/CloudStorage/OneDrive-Cisco/Documents/03_Funny_Projects/Project_Odin/git/odin && \
      ~/Library/Python/3.9/bin/splunk-appinspect inspect ODIN_app_for_splunk --mode precert --excluded-tags cloud --output-file .planning/artifacts/appinspect/odin-app-phase05-wave0-plan03.json --data-format json >/dev/null 2>&1 && \
      python3 -c "import json; r=json.load(open('.planning/artifacts/appinspect/odin-app-phase05-wave0-plan03.json'))['summary']; exit(0 if r['failure']+r['error']==0 else 1)" && \
      bash tools/tests/check-version-sync.sh && \
      bash tools/tests/check-two-app-split.sh && \
      bash tools/tests/injection-fixtures/run.sh && \
      bash tools/tests/windows-parity-harness.sh && \
      bash tools/tests/check-windows-classification.sh && \
      echo "T3 PASS"
    </automated_verify>
  </task>

verification:
  command_examples:
    - "grep -A 15 'alert_odin_fleet_scan_success_rate_below_slo' ODIN_app_for_splunk/default/savedsearches.conf"
    - "grep -c '^### Alert:' DOCS/RUNBOOK.md"
    - "wc -l DOCS/RUNBOOK.md"
---

# Plan 05-03 — Runbook + SLO Alert Stanza (PROD-03, Wave 0)

## Goal

Deliver PROD-03 by:
- Adding the 3rd alert stanza `alert_odin_fleet_scan_success_rate_below_slo` (definition-only, `disabled = 1` per D2)
- Writing `DOCS/RUNBOOK.md` with 4 alert-response entries (one per ROADMAP success criterion 1)

## Why Wave 0

PROD-03 has zero dependencies on other Phase 5 work. Runs in parallel with Plan 01 (PROD-07) and Plan 02 (PROD-06+05).

## Critical research findings (from 05-RESEARCH.md §3)

- Existing alert stanza pattern documented byte-for-byte; new SLO stanza mirrors it
- `description` field is the canonical operator-handoff documentation in savedsearches.conf
- SLO threshold (95%) is a starting recommendation; operator sets per-org after PROD-02 baseline

## Out of scope

- Activating the alerts (D2: stays disabled-only; activation is operator decision after PROD-02)
- Any other documentation files (PROD-04 owns INSTALL/TROUBLESHOOTING/DATA-DICTIONARY/UPGRADE — Plan 04)
- Module-side changes (Plan 01)
- Dashboard or rollback (Plan 02)

## Task summary

| # | Task | Output |
|---|---|---|
| 1 | Append SLO alert stanza to savedsearches.conf | 3rd `[alert_*]` stanza, definition-only |
| 2 | Create DOCS/RUNBOOK.md (≥100 lines, 4 entries × 4 subsections each) | Runbook covering all 4 alert types |
| 3 | AppInspect verification + Phase 1+2+3+4 regression check | All gates green |

## Threat model — see frontmatter
