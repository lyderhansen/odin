#
# Splunk app configuration file for TA-ODIN
# Discovery app for automatic log and service detection
#

[install]
is_configured = 0
build = 1

[ui]
is_visible = false
label = TA-ODIN Discovery

[launcher]
author = Your Organization
description = Technology Add-on for ODIN - Automatic discovery of logs and services on endpoints. Deploys via Deployment Server to identify what data sources exist on hosts without ingesting the actual logs.
version = 1.0.0

[package]
id = TA-ODIN
