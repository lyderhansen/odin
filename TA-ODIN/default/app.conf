#
# Splunk app configuration file for TA-ODIN v2.0
# Full endpoint enumeration and classification add-on
#

[install]
is_configured = 0
build = 2

[ui]
is_visible = false
label = TA-ODIN Endpoint Enumeration

[launcher]
author = Your Organization
description = Technology Add-on for ODIN - Full enumeration of services, ports, packages, scheduled tasks, processes, and mounts on endpoints. Reports metadata only. Classification lookups map raw data to host roles for automated onboarding.
version = 2.0.0

[package]
id = TA-ODIN
